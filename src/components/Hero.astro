---
const CALENDLY_URL = 'https://calendly.com/4clover';
---

<section
  class="relative overflow-hidden pt-20 pb-28 lg:pt-32 lg:pb-40 bg-background-light dark:bg-background-dark transition-colors duration-300"
  id="inicio"
>
  <!-- Background pattern -->
  <div class="absolute inset-0 hero-pattern opacity-40 dark:opacity-15 pointer-events-none"></div>

  <!-- Blur glow -->
  <div
    class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[700px] h-[700px] bg-primary/15 dark:bg-primary/20 rounded-full blur-[140px] pointer-events-none"
  ></div>

  <!-- Decorative floating circles -->
  <div class="absolute top-24 left-10 w-3 h-3 rounded-full bg-primary/40 animate-float hidden lg:block"></div>
  <div class="absolute top-40 right-16 w-2 h-2 rounded-full bg-emerald-400/60 animate-float hidden lg:block" style="animation-delay: 1s;"></div>
  <div class="absolute bottom-32 left-1/4 w-4 h-4 rounded-full bg-primary/30 animate-float hidden lg:block" style="animation-delay: 0.5s;"></div>

  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
    <div class="text-center">

      <!-- Badge -->
      <div class="animate-on-scroll inline-flex items-center px-4 py-2 rounded-full bg-primary/10 border border-primary/20 text-primary dark:text-emerald-400 font-medium text-sm mb-8">
        <span class="relative flex w-2 h-2 mr-2">
          <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-primary opacity-75"></span>
          <span class="relative inline-flex rounded-full w-2 h-2 bg-primary"></span>
        </span>
        Impulsando PYMEs Chilenas
      </div>

      <!-- Headline -->
      <h1 class="animate-on-scroll text-5xl md:text-6xl lg:text-7xl font-display font-bold tracking-tight text-text-light dark:text-white mb-6 leading-tight text-balance">
        Tecnología de nivel <br class="hidden md:block" />
        <span class="gradient-text">empresarial</span> para el crecimiento de tu empresa
      </h1>

      <!-- Subheadline -->
      <p class="animate-on-scroll mt-4 max-w-2xl mx-auto text-xl text-text-muted-light dark:text-gray-400 mb-10 text-balance">
        Llevamos Cloud Computing, Desarrollo de Software y Outsourcing TI de clase mundial a las empresas que mueven a Chile.
      </p>

      <!-- CTAs -->
      <div class="animate-on-scroll flex flex-col sm:flex-row justify-center gap-4">
        <a href="#contacto" class="btn-primary">
          Contáctanos
          <span class="material-icons ml-2 text-lg">arrow_forward</span>
        </a>

        <button
          id="hero-calendly-btn"
          class="btn-secondary"
          data-calendly-url={CALENDLY_URL}
        >
          <span class="material-icons mr-2 text-primary">calendar_today</span>
          Agenda una reunión
        </button>
      </div>

      <!-- Stats strip -->
      <div class="animate-on-scroll mt-20 grid grid-cols-3 gap-8 max-w-lg mx-auto">
        <div class="text-center">
          <p class="text-3xl font-display font-bold text-primary dark:text-emerald-400">+50</p>
          <p class="text-sm text-text-muted-light dark:text-gray-400 mt-1">Proyectos</p>
        </div>
        <div class="text-center border-x border-gray-200 dark:border-gray-700">
          <p class="text-3xl font-display font-bold text-primary dark:text-emerald-400">3</p>
          <p class="text-sm text-text-muted-light dark:text-gray-400 mt-1">Servicios Core</p>
        </div>
        <div class="text-center">
          <p class="text-3xl font-display font-bold text-primary dark:text-emerald-400">24h</p>
          <p class="text-sm text-text-muted-light dark:text-gray-400 mt-1">Respuesta</p>
        </div>
      </div>

    </div>
  </div>
</section>

<script>
  document.getElementById('hero-calendly-btn')?.addEventListener('click', () => {
    const btn = document.getElementById('hero-calendly-btn') as HTMLButtonElement;
    const url = btn.dataset.calendlyUrl || 'https://calendly.com/4clover';
    if (typeof (window as any).Calendly !== 'undefined') {
      (window as any).Calendly.initPopupWidget({ url });
    } else {
      window.open(url, '_blank');
    }
  });
</script>
