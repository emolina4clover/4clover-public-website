---
const values = [
  {
    icon: 'lightbulb',
    title: 'Innovación',
    description: 'Adoptamos las últimas tecnologías para ofrecer soluciones de vanguardia que mantengan a tu empresa competitiva.',
  },
  {
    icon: 'handshake',
    title: 'Compromiso',
    description: 'Nos involucramos en el éxito de cada cliente como si fuese nuestro propio negocio, acompañando cada etapa.',
  },
  {
    icon: 'verified',
    title: 'Excelencia',
    description: 'Altos estándares de calidad en cada entrega, con procesos de revisión y testing rigurosos.',
  },
  {
    icon: 'diversity_3',
    title: 'Equipo',
    description: 'Talento diverso y multidisciplinario que trabaja colaborativamente para superar cualquier desafío técnico.',
  },
];

const milestones = [
  { year: '2022', event: 'Fundación de 4Clover Technology en Santiago, Chile.' },
  { year: '2023', event: 'Primeros clientes empresariales y expansión del equipo de desarrollo.' },
  { year: '2024', event: 'Lanzamiento de servicios Cloud y asociación con Google y Azure.' },
  { year: '2025', event: 'Más de 50 proyectos entregados y apertura de nuevas verticales de negocio.' },
  { year: '2026', event: 'Expansión a Latinoamérica y consolidación del área de Outsourcing TI.' },
];
---

<section
  class="py-24 bg-background-light dark:bg-background-dark transition-colors duration-300"
  id="nosotros"
>
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">

    <!-- Header -->
    <div class="text-center mb-20 animate-on-scroll">
      <h2 class="text-base text-primary dark:text-emerald-400 font-semibold tracking-widest uppercase mb-3">
        Nosotros
      </h2>
      <p class="text-3xl sm:text-4xl font-display font-bold text-text-light dark:text-white leading-tight text-balance">
        Tecnología con propósito
      </p>
    </div>

    <!-- Mission / Vision grid -->
    <div class="grid lg:grid-cols-2 gap-16 mb-24 items-center">

      <!-- Image / visual side -->
      <div class="animate-on-scroll from-left">
        <div class="relative">
          <!-- Main image -->
          <div class="rounded-2xl overflow-hidden shadow-2xl aspect-[4/3]">
            <img
              src="https://images.unsplash.com/photo-1600880292203-757bb62b4baf?w=800&q=80"
              alt="Equipo 4Clover Technology"
              class="w-full h-full object-cover"
              loading="lazy"
              decoding="async"
            />
            <div class="absolute inset-0 bg-gradient-to-br from-primary/20 to-transparent mix-blend-overlay"></div>
          </div>

          <!-- Floating stat card -->
          <div class="absolute -bottom-6 -right-6 bg-white dark:bg-surface-dark rounded-2xl shadow-xl p-6 border border-gray-100 dark:border-gray-700 w-44">
            <p class="text-4xl font-display font-bold text-primary">+50</p>
            <p class="text-sm text-text-muted-light dark:text-gray-400 mt-1">Proyectos<br/>entregados</p>
          </div>

          <!-- Floating badge -->
          <div class="absolute -top-4 -left-4 bg-primary text-white rounded-xl shadow-lg p-4 w-36 text-center">
            <p class="text-2xl font-bold">5+</p>
            <p class="text-xs mt-0.5 text-white/80">Años de experiencia</p>
          </div>
        </div>
      </div>

      <!-- Text side -->
      <div class="animate-on-scroll from-right space-y-8">
        <div>
          <h3 class="text-2xl font-display font-bold text-text-light dark:text-white mb-4 flex items-center gap-3">
            <span class="w-8 h-8 rounded-lg bg-primary/10 text-primary flex items-center justify-center">
              <span class="material-icons text-lg">flag</span>
            </span>
            Nuestra Misión
          </h3>
          <p class="text-text-muted-light dark:text-gray-400 leading-relaxed">
            Democratizar el acceso a tecnología de nivel empresarial para las PYMEs chilenas, entregando soluciones innovadoras, accesibles y de alto impacto que impulsen el crecimiento sostenible de cada negocio.
          </p>
        </div>

        <div>
          <h3 class="text-2xl font-display font-bold text-text-light dark:text-white mb-4 flex items-center gap-3">
            <span class="w-8 h-8 rounded-lg bg-primary/10 text-primary flex items-center justify-center">
              <span class="material-icons text-lg">visibility</span>
            </span>
            Nuestra Visión
          </h3>
          <p class="text-text-muted-light dark:text-gray-400 leading-relaxed">
            Ser el principal aliado tecnológico de las empresas en Chile y Latinoamérica, reconocidos por nuestra excelencia técnica, cercanía humana y por el impacto real que generamos en el éxito de nuestros clientes.
          </p>
        </div>

        <a href="#contacto" class="btn-primary w-fit">
          Trabajemos juntos
          <span class="material-icons ml-2 text-lg">arrow_forward</span>
        </a>
      </div>

    </div>

    <!-- Values -->
    <div class="mb-24">
      <h3 class="text-2xl font-display font-bold text-text-light dark:text-white text-center mb-12 animate-on-scroll">
        Nuestros valores
      </h3>
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
        {values.map((value, i) => (
          <div
            class="stagger-item text-center p-6 rounded-2xl bg-white dark:bg-surface-dark border border-gray-100 dark:border-gray-800 hover:border-primary/30 hover:shadow-lg transition-all duration-300 group"
            style={`transition-delay: ${i * 100}ms`}
          >
            <div class="w-14 h-14 rounded-2xl bg-primary/10 flex items-center justify-center mx-auto mb-4 text-primary group-hover:bg-primary group-hover:text-white group-hover:scale-110 transition-all duration-300">
              <span class="material-icons text-2xl">{value.icon}</span>
            </div>
            <h4 class="font-bold text-text-light dark:text-white mb-2">{value.title}</h4>
            <p class="text-sm text-text-muted-light dark:text-gray-400 leading-relaxed">{value.description}</p>
          </div>
        ))}
      </div>
    </div>

    <!-- Timeline -->
    <div class="animate-on-scroll">
      <h3 class="text-2xl font-display font-bold text-text-light dark:text-white text-center mb-12">
        Nuestra historia
      </h3>
      <div class="relative">
        <!-- Line -->
        <div class="absolute left-1/2 -translate-x-0.5 top-0 bottom-0 w-0.5 bg-gray-200 dark:bg-gray-700 hidden md:block"></div>

        <div class="space-y-8">
          {milestones.map((milestone, i) => (
            <div class={`stagger-item flex flex-col md:flex-row gap-6 items-start md:items-center ${i % 2 === 0 ? 'md:flex-row' : 'md:flex-row-reverse'}`} style={`transition-delay: ${i * 100}ms`}>
              <div class={`flex-1 ${i % 2 === 0 ? 'md:text-right' : 'md:text-left'}`}>
                <div class={`bg-white dark:bg-surface-dark rounded-xl p-5 border border-gray-100 dark:border-gray-800 shadow-sm inline-block ${i % 2 === 0 ? 'md:ml-auto' : 'md:mr-auto'} max-w-xs w-full`}>
                  <span class="text-primary dark:text-emerald-400 font-bold text-sm">{milestone.year}</span>
                  <p class="text-text-light dark:text-white text-sm mt-1">{milestone.event}</p>
                </div>
              </div>

              <!-- Dot -->
              <div class="hidden md:flex w-4 h-4 rounded-full bg-primary border-4 border-background-light dark:border-background-dark flex-shrink-0 z-10"></div>

              <div class="flex-1"></div>
            </div>
          ))}
        </div>
      </div>
    </div>

  </div>
</section>
